
#include <stdio.h>

int main()
{ 
    int num1 = 100;
    int num2 = 2;
    int soma = num1 + num2;
    int resto = num1 % num2;
    int divisao = num1 / num2;
    int multiplica = num1 * num2;
    printf("Soma de 2 numeros 100 e 2: %d \n", soma );
    printf("Resto de 2 numeros 100 e 2: %d \n", resto );
    printf("divisao de 2 numeros 100 e 2: %d \n", divisao );
    printf("multiplica de 2 numeros 100 e 2: %d \n", multiplica );
    

    return 0;
}
